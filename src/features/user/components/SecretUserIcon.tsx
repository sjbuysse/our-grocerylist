import React from 'react';
import Icon from '../../../core/components/Icon/Icon';
import { ICONS } from '../../../constants/constants';

const SecretUserIcon = () => (
    <Icon icon={ICONS.SECRET_USER} color={'white'} size={20} />
)

export default SecretUserIcon;
